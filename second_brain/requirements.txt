# 第二大脑服务（在 Mac mini 上运行）依赖
openai>=1.30.0          # 调用 DeepSeek-V4-Pro
fastembed>=0.3.0        # 本地免费向量（RAG + 记忆检索）
numpy>=1.26.0
lark-oapi>=1.4.0        # 飞书官方 SDK（长连接接入）
gradio>=4.0.0           # 网页版聊天界面（webapp.py，可选）
